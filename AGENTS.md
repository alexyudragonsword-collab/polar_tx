# polartx 协作规则

> 本项目使用 Project Cairn 组织项目知识：`AGENTS.md` 是规则与导航入口，`cairn/` 是项目知识/状态层。同目录的 `CLAUDE.md` 只有一行 `@AGENTS.md`，让 Claude Code 读到同一套规则；Codex 直接读本文件。由 `cairn init` 生成，已填入本项目自身的定位与配置。

## 项目一句话

`polartx` — 数字极坐标发射机（digital polar TX）行为级仿真库：窄带 ADPLL 两点调制 + 数字 PA（BLE / BT EDR / EDGE / LTE ≤20 MHz），宽带开环 DTC 相位调制 + 数字 PA（WiFi 6/7 ≤320 MHz、5G NR ≤200 MHz）。这是物理库，不是应用——这里值得抓的 bug 几乎都是**数字算错了**，不是崩溃。

## Init 配置

- 毕业 provider：暂缓对接（deferred——第一次真要毕业知识时再接知识库）；知识库索引与毕业目标：尚未配置。

## 进入项目后的阅读顺序

先读本文件 → 读 `cairn/LOG.md` 最新几条（新的在最上面）→ 按任务读相关 `cairn/` 知识专题文档 → 改实质内容前先看 `chain.py`、`phasemod/`（`base.py` + 对应调制器）、`presets.py`、`guiutil.py`。

## 文档地图与职责

**本文件只做规则与导航，操作细节一律去下面这些文件里查。** 面向使用者的文档（上半）和 `cairn/` 知识层（下半）是两套，不要混。

| 文件 | 角色 | 维护方式 |
|---|---|---|
| `CONTRIBUTING.md` | **命令、环境、提交规矩、约定全文**（下面"硬规矩"的完整版） | 约定变了才改 |
| `docs/architecture.md` | 模块地图、数据流三个对象、"加东西改哪里" | 结构变了才改；改代码前必读 |
| `README.md` | 能力、结果表、建模口径与已知边界 | 能力或数字变了就改 |
| `CHANGELOG.md` / `ROADMAP.md`（根） | 历史 / 未完成项 | 条目做完：ROADMAP 删掉、CHANGELOG 记一笔。本项目**不建** `cairn/ROADMAP.md` |
| `AGENTS.md`（根） | 规则与导航 | 很少变；控制在 ~65 行内 |
| `CLAUDE.md`（根） | 一行 `@AGENTS.md` 存根 | 写一次，之后不动 |
| `cairn/LOG.md` | 时序日志 | 新条目加在最上面，每条 ≤20 行，只写摘要 + 指针 |
| `cairn/<topic>.md` | 知识专题文档（当前真相） | 原地更新；坑写进正文并用 `contains` 打标；修订留 LOG 指针 |
| `cairn/Reference/` · `cairn/Cited.md` | 外部原始输入 · 知识库引用清单 | 按需创建；只追加 · 只放指针，绝不复制原文 |

> `cairn/` 下其余文件只在有明确信号时才建（需要记一个决策、填掉一个坑、出现跨会话的目标），不预建空壳。工程资产（被代码或流程消费的契约/配置/规格）不归本系统管，留在代码树里。

## 冲突仲裁规则

- **代码事实 > 一切文档**。这是物理库，数字是承重的：文档和实测/测试冲突时以跑出来的数字为准，然后修文档。
- 文档之间：**知识专题文档 > LOG 历史**（结论以 `cairn/` 专题里的最新记录为准，不以更早的 LOG 条目为准）；规则层面的冲突由本文件裁定。

## 知识库消费反射

开工前，如果这项工作的可复用内核——它产出或依赖的任何结论——够格毕业，先查本项目自己的 `cairn/` 知识专题文档；目前尚未对接外部知识库（provider 暂缓对接，见上面 Init 配置），所以外部索引检查与 `cairn/Cited.md` 引用在接上知识库后才启用。

## 文档协作规则

- 动手改之前，先判断用户要的是"讨论/建议"还是"直接改文档"；说"先看看/先评估"时先给分析，不要直接重写正式文档。
- 更正过去的判断时追加更正说明，不要无声覆盖；未经确认的判断不要写成既定事实。

## 本仓库特有的硬规矩

完整版在 `CONTRIBUTING.md`（每条都有测试或 CI job 背书），这里只列最容易破坏的：

- **断言物理量，不断言快照**。不要把当前输出硬编码成期望值，也不要硬编码计数。
- **`src/polartx/vendor/` 是可校验的改编副本，不是 fork**。改它之前先想能不能在 `polartx` 侧包一层；真要改就更新 `tools/vendor_manifest.json`。
- **两个 GUI 同步，或者都别动**。计算住在 `guiutil.py`，Streamlit 与 PySide6 两个前端都要接、都要测。
- **测量口径要显式声明**。EVM 均衡（`scalar` vs `per_tone`）由 `PolarResult.evm_equalize_default` 承载，保证画的星座图和打印的数字同口径；mask 的出处由 `MaskSpec.source` 承载。
- **绝不臆造规范限值**。内置 mask 全是 `source="stylized"` 的工程模板；需要真表格就开口要，不要编看起来合理的数字。
- **引擎有适用域**（`ADPLLTwoPoint` 的 `mode="event"` 只在每参考周期相位推进 ≪1 UI 时有效，越界告警并暴露 `ui_per_ref_cycle_p99`）；`response` 模式已线性化，**不能背书杂散结论**。指标输入不够时**抛异常**，不返回 NaN。
- 只在 `claude/digital-polar-tx-dev-r0c338` 上开发提交；不主动推 `main`、不主动开 PR；提交信息写**为什么**。GitHub 走 `mcp__github__*`，没有 `gh`。

## 知识沉淀规则

- 每次实质性进展之后，在 `cairn/LOG.md` 顶部加一条（摘要 + 指针）；结论沉淀进 `cairn/` 知识专题文档。
- **完成回复闸门**：在任何完成性表述之前——包括但不限于工作已完成/已实现、已定稿、已更新、已同步、已验证或测试通过；问题已修复或已解决；交付物可用；工作已结束的表述，以及语义等价的说法——先跑 `references/maintenance.md` 里的 Cairn 检查点，只更新其触发矩阵要求的记录，核对后再回复。明确的只读/不要改动要求禁止任何 Cairn 写入。
- 跨项目可复用的经验，在接上知识库之后通过毕业机制沉淀（provider 暂缓对接，见上面 Init 配置）。
